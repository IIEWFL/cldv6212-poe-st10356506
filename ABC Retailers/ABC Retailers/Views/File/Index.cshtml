@* @model IEnumerable<string>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* view for displaying the files */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
        }

        th {
            background-color: #f4f4f4;
        }
    </style>
</head>
<body>
    <h2>Uploaded Files</h2>
    button for uploading the file, button redirects user to the NewFile page which allows users to upload files 
    <a href="@Url.Action("NewFile", "File")" class="btn btn-primary mb-3">Upload New File</a>
    <table>
        <thead>
            <tr>
                <th>File Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Any())
            {
                @foreach (var fileName in Model)
                {
                   @* buttons for uploading and downloading files 
                    <tr>
                        <td>@fileName</td>
                        <td>
                            <a href="@Url.Action("DownloadFile", "File", new { fileName })"class="btn btn-primary mb-3">Download</a>
                            <a href="@Url.Action("DeleteFile", "File", new { fileName })"class="btn btn-danger mb-3">Delete</a>
                        </td>
                    </tr>
                }
            }
            else
            {
               @*  if no files are uploaded 
                <tr>
                    <td colspan="2">No files uploaded yet.</td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
 *@