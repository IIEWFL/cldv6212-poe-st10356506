@* @{
    ViewData["Title"] = "Add New Contract";
}
@* form for uploading a new file 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload New Contract</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <h2>Upload New File</h2>
    <p class="lead">Select a file to upload.</p>
    <form method="post" enctype="multipart/form-data" action="@Url.Action("UploadFile", "File")">
        <div class="mb-3">
            <label for="file" class=" form-label mt-4">Choose File:</label>
            <input class="form-control" type="file" id="file" name="file" required />
        </div>
        <button type="submit" class=" btn btn-success">Upload</button>
    </form>
</body>
</html> *@
